'use strict';

var node_module = require('node:module');
var _default = require('./default.cjs');

var _documentCurrentScript = typeof document !== 'undefined' ? document.currentScript : null;
const require$1 = node_module.createRequire((typeof document === 'undefined' ? require('u' + 'rl').pathToFileURL(__filename).href : (_documentCurrentScript && _documentCurrentScript.src || new URL('tailwindcss.cjs', document.baseURI).href)));
const { plugins, ...defaultOptions } = _default;
var tailwindcss = {
  ...defaultOptions,
  plugins: [...plugins, require$1.resolve("prettier-plugin-tailwindcss")],
  tailwindFunctions: [
    "classnames",
    "clsx",
    "cn",
    "ctl",
    "cva",
    "twJoin",
    "twMerge",
    "tv"
  ]
};

module.exports = tailwindcss;

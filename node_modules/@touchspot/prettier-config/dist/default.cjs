'use strict';

var node_module = require('node:module');

var _documentCurrentScript = typeof document !== 'undefined' ? document.currentScript : null;
const require$1 = node_module.createRequire((typeof document === 'undefined' ? require('u' + 'rl').pathToFileURL(__filename).href : (_documentCurrentScript && _documentCurrentScript.src || new URL('default.cjs', document.baseURI).href)));
var config = {
  plugins: [
    require$1.resolve("prettier-plugin-packagejson"),
    require$1.resolve("prettier-plugin-toml")
  ]
};

module.exports = config;
